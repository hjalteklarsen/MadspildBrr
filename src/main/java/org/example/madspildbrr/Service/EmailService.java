package org.example.madspildbrr.Service;
/*
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;
import org.springframework.mail.SimpleMailMessage;
import org.springframework.mail.javamail.JavaMailSender;

@Service
public class EmailService {

    @Autowired
    private JavaMailSender mailSender;

    public void sendConfirmation(String to, String name, int participants) {
        SimpleMailMessage message = new SimpleMailMessage();
        message.setTo(to);
        message.setSubject("Tilmelding modtaget");
        message.setText("Tak for tilmeldingen, vi vender tilbage når et menneske har set det.\n\n" +
                "Navn: " + name + "\n" +
                "Antal deltagere: " + participants);

        mailSender.send(message);
    }
}
*/